global:
  environment: production
  domainName: prod.iviss.skyengpro.app
  imageTag: latest

replicas: 3

hpa:
  minReplicas: 3
  maxReplicas: 10

config:
  environment: production
  logLevel: info
  smsProvider: vonage
  emailProvider: resend
  otpViaEmail: true

ingress:
  hosts:
    - host: prod.iviss.skyengpro.app
      paths:
        - /api(/|$)(.*)
        - /api-doc(/|$)(.*)
  tls:
    - hosts:
        - prod.iviss.skyengpro.app
      secretName: iviss-api-tls