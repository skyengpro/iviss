FROM node:20-alpine

WORKDIR /app

# Only install prod dependencies for the metrics server
COPY package.json package-lock.json ./
RUN npm ci --omit=dev 2>/dev/null || npm install --production

COPY metrics-server.js ./

EXPOSE 9091

CMD ["node", "metrics-server.js"]
