# ============================================
# IVISS Production Environment (Auto-generated by Ansible)
# ============================================

# PostgreSQL - Internal Database
POSTGRES_USER={{ db_user | default('iviss_user') }}
POSTGRES_PASSWORD={{ db_password }}
POSTGRES_DB={{ db_name | default('iviss_prod') }}

# PostgreSQL - External Database (placeholder)
EXTERNAL_POSTGRES_USER={{ db_user | default('iviss_user') }}
EXTERNAL_POSTGRES_PASSWORD={{ db_password }}
EXTERNAL_POSTGRES_DB={{ db_name | default('iviss_prod') }}
EXTERNAL_POSTGRES_HOST=iviss-db
EXTERNAL_POSTGRES_PORT=5432

# JWT Authentication
JWT_PRIVATE_KEY_PEM="{{ jwt_private_key_pem }}"
JWT_PUBLIC_KEY_PEM="{{ jwt_public_key_pem }}"
ACTIVATION_CODE_PEPPER="{{ activation_code_pepper }}"

# Backend Configuration
ENVIRONMENT={{ app_environment | default('staging') }}
SERVER_HOST=0.0.0.0
SERVER_PORT=3000
RUST_LOG=info

# Admin Bootstrap
ADMIN_BOOTSTRAP_EMAIL={{ admin_bootstrap_email }}
ADMIN_BOOTSTRAP_PASSWORD={{ admin_bootstrap_password }}
ADMIN_BOOTSTRAP_PHONE={{ admin_bootstrap_phone }}
ADMIN_BOOTSTRAP_USERNAME={{ admin_bootstrap_username }}

# Twilio Configuration
TWILIO_ACCOUNT_SID={{ twilio_account_sid | default('mock') }}
TWILIO_AUTH_TOKEN={{ twilio_auth_token | default('mock') }}
TWILIO_FROM_NUMBER={{ twilio_from_number | default('mock') }}

